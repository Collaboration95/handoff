[project]
name = "handoff-finance"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["openai>=1", "pydantic>=2", "python-dotenv>=1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
